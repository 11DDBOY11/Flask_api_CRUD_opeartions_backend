# Flask_api_CRUD_opeartions_backend
A simple Student Management REST API built using Flask and SQLite that performs CRUD operations like adding, viewing, updating, and deleting student records. It demonstrates basic backend development concepts such as API routing, database integration, and JSON handling in Python.
